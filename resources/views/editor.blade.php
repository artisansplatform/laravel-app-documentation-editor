<div id="document-editor-root" data-content="{{ htmlspecialchars(json_encode($content)) }}" data-options="{{ htmlspecialchars(json_encode($options)) }}">
    <!-- Document editor will be mounted here -->
</div>
